import type { IState, Action } from './context';
import type { ITodo, ITodos, } from 'types/todo';
import { TStatus, TCategory, TPriority } from 'types/todo';

export type { 
  IState, 
  Action, 
  ITodo,
  ITodos,
};

export {
  TStatus, 
  TCategory, 
  TPriority 
}
